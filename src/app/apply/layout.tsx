import type { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'Apply for Wholesale | Sweet Shop USA Wholesale',
    description:
        'Apply to become a Sweet Shop USA wholesale partner. Handmade chocolates for retailers, shops, and businesses.',
};

export default function ApplyLayout({ children }: { children: React.ReactNode }) {
    return children;
}
