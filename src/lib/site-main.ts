/** Primary content landmark for skip navigation (`#${SITE_MAIN_ID}`). */
export const SITE_MAIN_ID = 'site-main';

/** Focus ring when moving focus to `<main>` via skip link (`tabIndex={-1}`). */
export const SITE_MAIN_FOCUS_CLASS =
    'scroll-mt-24 outline-none focus:ring-2 focus:ring-[#c49a78] focus:ring-offset-2 focus:outline-none';
