/** Cookie storing which wholesale account catalog scope is active (user-owned account id). */
export const WHOLESALE_SELECTED_ACCOUNT_COOKIE = 'wholesale_selected_account';

/** Set when an admin explicitly shops on behalf of another account (manage “Shop as” or header search). */
export const WHOLESALE_ADMIN_SHOP_AS_COOKIE = 'wholesale_admin_shop_as';
